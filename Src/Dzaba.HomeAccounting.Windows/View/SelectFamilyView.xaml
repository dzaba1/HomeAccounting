<UserControl x:Class="Dzaba.HomeAccounting.Windows.View.SelectFamilyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dzaba.HomeAccounting.Windows.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <TextBox Margin="3" VerticalAlignment="Center" HorizontalAlignment="Stretch"
                 Grid.Row="0" Grid.Column="0"
                 Text="{Binding Path=NewFamilyName, UpdateSourceTrigger=PropertyChanged}"/>
        <Button Margin="3" VerticalAlignment="Center" HorizontalAlignment="Right"
                Grid.Row="0" Grid.Column="1" MinWidth="60"
                Content="Nowa"
                Command="{Binding Path=NewCommand}" />
    </Grid>
</UserControl>
